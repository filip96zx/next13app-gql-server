input WhereInput {
	slug: String,
	
}

extend type Query {
	category(id: ID!): Category
	categories(skip: Int, take: Int, where: WhereInput): [Category!]!
}

type Category {
	id: ID!
	name: String
	slug: String
	products: [Product]
}
